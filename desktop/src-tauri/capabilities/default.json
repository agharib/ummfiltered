{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the UmmFiltered desktop window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
